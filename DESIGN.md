# SciSiam Web Application - Design System & Tokens

เอกสารนี้รวบรวมค่ากำหนดทางดีไซน์ (Design Tokens) และระเบียบการจัดองค์ประกอบภาพลักษณ์สำหรับโปรเจ็กต์ **SciSiam** เพื่อรักษาทิศทางความสวยงามและความพรีเมียมให้สอดคล้องกันทุกหน้าจอ

---

## 1. Color Palette (ระบบโทนสีหลัก)

เราเน้นใช้โทนสีขาวสะอาดสไตล์ห้องปฏิบัติการ (Clean Lab) ผสมผสานกับความล้ำสมัยสไตล์แก้วสะท้อนแสง (Glassmorphic) และคุมโทนสีประจำวิชาทางวิทยาศาสตร์อย่างชัดเจน:

*   **พื้นหลังหลัก (Background)**: `#f8fafc` (Slate 50) และไล่เฉดเข้าหา `#f1f5f9` (Slate 100)
*   **พื้นหลังการ์ด (Card Background)**: `rgba(255, 255, 255, 0.7)` (ขาวแก้วกึ่งโปร่งใส)
*   **ข้อความหลัก (Text Colors)**:
    *   หัวข้อ (Headers): `#0f172a` (Slate 900)
    *   คำบรรยาย (Body): `#475569` (Slate 600)
    *   ข้อความสีจาง (Muted): `#94a3b8` (Slate 400)
*   **เฉดสีประจำกลุ่มวิชาจำลอง (Subject Identity)**:
    *   **ฟิสิกส์ (Physics)**: สีฟ้าสว่าง (Cyan/Sky) -> `#3b82f6` (Blue 500) และ `#0ea5e9` (Sky 500)
    *   **เคมี (Chemistry)**: สีเขียวมรกต (Emerald/Teal) -> `#10b981` (Emerald 500) และ `#14b8a6` (Teal 500)
    *   **ชีววิทยา (Biology)**: สีเขียวสดใส (Green/Lime) -> `#22c55e` (Green 500)

---

## 2. Typography (การออกแบบตัวอักษร)

เราโหลดฟอนต์ **'Prompt'** ร่วมกับ **'Inter'** เป็นหลัก โดยใช้โครงสร้างดังนี้:

*   **Sans-Serif Font Family**: `'Prompt', 'Inter', sans-serif`
*   **ระยะห่างระหว่างบรรทัดภาษาไทย (Thai Line Height)**:
    *   **ห้ามเด็ดขาด**: ห้ามปล่อยให้ `line-height` มีความหนาแน่นเท่ากับ 1.0 หรือ 1.2 ในวรรคภาษาไทย
    *   **ค่ามาตรฐาน**: ใช้คลาส `leading-relaxed` (1.625) เป็นตัวตั้งหลักสำหรับย่อหน้าทั่วไป หรือกำหนด `leading-[1.5]` ถึง `leading-[1.7]`
*   **ระยะช่องไฟอักษร (Letter Spacing)**:
    *   ห้ามกำหนดระยะห่างตัวอักษรเป็นค่าติดลบ (เช่น `tracking-tight` หรือ `tracking-tighter`) บนภาษาไทยเด็ดขาด
    *   ให้ใช้ค่ามาตรฐาน (`tracking-normal`) หรือกว้างขึ้นเล็กน้อย (`tracking-wide`) สำหรับหัวข้อขนาดใหญ่

---

## 3. Glassmorphism & Elevation (มิติพื้นผิวแก้ว)

เพื่อให้อินเตอร์เฟสรู้สึกพรีเมียมเสมือนหน้าจอ Dashboard โฮโลแกรมในห้องปฏิบัติการจริง:

*   **เอฟเฟกต์กระจกฝ้า (Glass Effect)**:
    *   ใช้ฉากหลังโปร่งแสงผสมผสานกับฟิลเตอร์เบลอ:
        ```css
        background: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.4);
        ```
*   **ขอบการ์ดโปร่งแสง (Double Border Highlight)**:
    *   ให้ขอบการ์ดมีความโปร่งแสงบางเบา เพื่อให้วัตถุที่ผ่านด้านหลังการ์ดลอยทะลุขึ้นมาได้สวยงาม
*   **เงาเรืองแสงด้านหลังวัตถุ (Back Glow Spots)**:
    *   ใส่กล่องเรืองแสงโปร่งใสในพื้นหลังสีสันประจำแล็บ (เช่น `box-shadow: 0 10px 30px -10px rgba(59, 130, 246, 0.2)`) เพื่อช่วยขับเน้นมิติตัวการ์ดให้น่าสนใจ

---

## 4. Transitions & Micro-Animations (ความลื่นไหลระดับ 60fps)

*   **Hover Effect**: ทุกปุ่มกดและการ์ดแบบ Interactive ต้องมีการเปลี่ยนผ่านที่นุ่มนวล:
    *   ใช้คลาส: `transition-all duration-200 ease-in-out`
    *   ขยายขนาดเล็กน้อยตอนชี้เมาส์: `hover:scale-[1.015]` และยุบตัวตอนกด: `active:scale-[0.985]`
*   **ประสิทธิภาพการรันกราฟิก (Animation Performance)**:
    *   สำหรับการขยับขอบเขตวัตถุ ให้ใช้คลาส CSS ที่เรียกใช้งาน GPU เสมอ หลีกเลี่ยงคลาสที่บังคับให้บราวเซอร์ทำ Re-layout เช่น การปรับปรุง `left` / `top` แบบดิบๆ
